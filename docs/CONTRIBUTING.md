# Contributing

- Run tests locally with: bash tools/run-all-tests.sh
- Lint code with: bash tools/lint-all.sh
- CI runs on GitHub Actions (Node v18, Python 3.x) and executes the same test suite.
- For new services, add a package.json with npm test script or a Python setup with pytest as appropriate.
